# FastAPI Configuration
# Add any environment variables needed for the encoder service here
# Currently, the encoder service doesn't require any specific environment variables
# but this file is created for future configuration needs

# Example:
# API_KEY=your_api_key_here
# DEBUG=true
